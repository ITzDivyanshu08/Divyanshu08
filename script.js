function handleClick(option) {
    switch(option) {
        case 1:
            alert("Pterodactyl Setup Selected!");
            break;
        case 2:
            alert("Hydra Panel Installation Selected!");
            break;
        case 3:
            alert("Making VPS 24/7 Selected!");
            break;
        default:
            alert("Unknown option!");
    }
}
